<Window xmlns:Controls="clr-namespace:Upsilon.Apps.Passkey.GUI.WPF.Views.Controls"
        x:Class="Upsilon.Apps.Passkey.GUI.WPF.Views.UserSettingsView"
        x:ClassModifier="internal"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Upsilon.Apps.Passkey.GUI.WPF.Views"
        xmlns:viewmodels="clr-namespace:Upsilon.Apps.Passkey.GUI.WPF.ViewModels"
        d:DataContext="{d:DesignInstance Type=viewmodels:UserSettingsViewModel}"
        mc:Ignorable="d"
        Title="{Binding Title}"
        ResizeMode="CanMinimize"
        WindowStartupLocation="CenterScreen"
        Height="520" Width="920">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Menu Grid.Row="0">
            <Menu.ItemsPanel>
                <ItemsPanelTemplate>
                    <DockPanel HorizontalAlignment="Stretch"/>
                </ItemsPanelTemplate>
            </Menu.ItemsPanel>
            <MenuItem Header="_Save"
                      Click="_save_MenuItem_Click"/>
            <MenuItem Name="_import_MI"
                      Header="_Import"
                      Click="_import_MenuItem_Click"/>
            <MenuItem Name="_export_MI"
                      Header="_Export">
                <MenuItem Header="Export as _JSON (Settings and Services)"
                          Click="_export_json_MenuItem_Click"/>
                <MenuItem Header="Export as _CSV (Services only)"
                          Click="_export_csv_MenuItem_Click"/>
            </MenuItem>
            <MenuItem Name="_viewActivities_MI"
                      Header="View Activities"
                      Click="_viewActivities_MenuItem_Click"/>
            <MenuItem Name="_deleteUser_MI"
                      Header="_Delete User"
                      HorizontalAlignment="Right"
                      Click="_deleteUser_MenuItem_Click"/>
        </Menu>

        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
        <StackPanel Orientation="Horizontal">
            <GroupBox Header="Credentials">
                <StackPanel>
                    <StackPanel Orientation="Horizontal">
                        <Label>Username :</Label>
                        <TextBox Name="_username_TB"
                                 Width="400"
                                 Text="{Binding Username, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
                    </StackPanel>
                    <Controls:UserPasswordsContainer x:Name="_passwordsContainer"/>
                </StackPanel>
            </GroupBox>
            <StackPanel>
            <GroupBox Header="Settings">
                <StackPanel>
                    <StackPanel Orientation="Horizontal">
                        <CheckBox IsChecked="{Binding LogoutTimeoutChecked, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                  Content="Logout after"/>
                        <TextBox Width="25"
                                 HorizontalContentAlignment="Center"
                                 TextChanged="_value_TextBox_TextChanged"
                                 PreviewTextInput="_value_TextBox_PreviewTextInput"
                                 DataObject.Pasting="_value_TextBox_Pasting"
                                 Text="{Binding LogoutTimeout, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
                        <Label Margin="0">minutes.</Label>
                    </StackPanel>
                    <StackPanel Orientation="Horizontal">
                        <CheckBox IsChecked="{Binding CleaningClipboardTimeoutChecked, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                  Content="Clean any passwords in clipboard every"/>
                        <TextBox Width="35"
                                 HorizontalContentAlignment="Center"
                                 TextChanged="_value_TextBox_TextChanged"
                                 PreviewTextInput="_value_TextBox_PreviewTextInput"
                                 DataObject.Pasting="_value_TextBox_Pasting"
                                 Text="{Binding CleaningClipboardTimeout, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
                        <Label Margin="0">seconds.</Label>
                    </StackPanel>
                    <StackPanel Orientation="Horizontal">
                        <CheckBox IsChecked="{Binding ShowPasswordDelayChecked, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                  Content="Show QR Code during"/>
                        <TextBox Width="40"
                                 HorizontalContentAlignment="Center"
                                 TextChanged="_value_TextBox_TextChanged"
                                 PreviewTextInput="_value_TextBox_PreviewTextInput"
                                 DataObject.Pasting="_value_TextBox_Pasting"
                                 Text="{Binding ShowPasswordDelay, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
                        <Label Margin="0">milliseconds.</Label>
                    </StackPanel>
                    <StackPanel Orientation="Horizontal">
                        <CheckBox IsChecked="{Binding NumberOfOldPasswordToKeepChecked, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                  Content="Limit password history to the previous"/>
                        <TextBox Width="40"
                                 HorizontalContentAlignment="Center"
                                 TextChanged="_value_TextBox_TextChanged"
                                 PreviewTextInput="_value_TextBox_PreviewTextInput"
                                 DataObject.Pasting="_value_TextBox_Pasting"
                                 Text="{Binding NumberOfOldPasswordToKeep, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
                        <Label Margin="0">entries.</Label>
                    </StackPanel>
                    <StackPanel Orientation="Horizontal">
                        <CheckBox IsChecked="{Binding NumberOfMonthActivitiesToKeepChecked, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                  Content="Limit activity history to the previous"/>
                        <TextBox Width="40"
                                 HorizontalContentAlignment="Center"
                                 TextChanged="_value_TextBox_TextChanged"
                                 PreviewTextInput="_value_TextBox_PreviewTextInput"
                                 DataObject.Pasting="_value_TextBox_Pasting"
                                 Text="{Binding NumberOfMonthActivitiesToKeep, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
                        <Label Margin="0">months.</Label>
                    </StackPanel>
                    <GroupBox Header="Warnings">
                        <StackPanel>
                            <CheckBox IsChecked="{Binding NotifyActivityReview, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}">Notify Activity Review</CheckBox>
                            <CheckBox IsChecked="{Binding NotifyDuplicatedPasswords, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}">Notify Duplicated Passwords</CheckBox>
                            <CheckBox IsChecked="{Binding NotifyPasswordUpdateReminder, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}">Notify Password Update Reminder</CheckBox>
                            <CheckBox IsChecked="{Binding NotifyPasswordLeaked, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}">Notify Password Leaked</CheckBox>
                        </StackPanel>
                    </GroupBox>
                </StackPanel>
            </GroupBox>
            <GroupBox Header="Application — Offline leak database"
                      Margin="0,8,0,0"
                      ToolTip="Machine-wide setting shared by all vault users. Not stored in the .pku vault. Disable keeps the file; only Delete removes it.">
                <StackPanel Margin="4">
                    <TextBlock TextWrapping="Wrap" Margin="0,0,0,6"
                               Text="{Binding OfflineLeakFilterStatus}"/>
                    <CheckBox Content="Use offline Bloom filter when HIBP and XposedOrNot are unreachable"
                              IsChecked="{Binding OfflineLeakFilterEnabled}"
                              IsEnabled="{Binding OfflineLeakFilterIdle}"
                              Checked="_offlineLeakFilterEnabled_Changed"
                              Unchecked="_offlineLeakFilterEnabled_Changed"/>
                    <TextBlock TextWrapping="Wrap" Margin="0,4,0,4" Foreground="Gray"
                               Text="{Binding OfflineLeakFilterProgress}"/>
                    <StackPanel Orientation="Horizontal" Margin="0,4,0,0">
                        <Button Content="Build / Update"
                                Margin="0,0,8,0"
                                Padding="8,2"
                                IsEnabled="{Binding OfflineLeakFilterIdle}"
                                Click="_offlineLeakFilterBuild_Click"/>
                        <Button Content="Delete offline database"
                                Padding="8,2"
                                IsEnabled="{Binding OfflineLeakFilterIdle}"
                                Click="_offlineLeakFilterDelete_Click"/>
                    </StackPanel>
                </StackPanel>
            </GroupBox>
            </StackPanel>
        </StackPanel>
        </ScrollViewer>
    </Grid>
</Window>
