<Window x:Class="Upsilon.Apps.Passkey.GUI.WPF.Views.InsertIdentifierView"
        x:ClassModifier="internal"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Upsilon.Apps.Passkey.GUI.WPF.Views"
        xmlns:viewmodels="clr-namespace:Upsilon.Apps.Passkey.GUI.WPF.ViewModels"
        xmlns:loc="clr-namespace:Upsilon.Apps.Passkey.GUI.WPF.Localization"
        d:DataContext="{d:DesignInstance Type=viewmodels:InsertIdentifierViewModel}"
        mc:Ignorable="d"
        Title="{loc:Loc Title_InsertIdentifier}"
        ResizeMode="CanMinimize"
        WindowStartupLocation="CenterScreen"
        Height="300" Width="400">
   <Grid>
      <Grid.RowDefinitions>
         <RowDefinition Height="Auto"/>
         <RowDefinition Height="Auto"/>
         <RowDefinition Height="*"/>
      </Grid.RowDefinitions>
      <StackPanel Grid.Row="0"
                  Orientation="Horizontal">
         <TextBox Name="_identifier_TB"
                  Width="330"
                  Text="{Binding Identifier, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                  KeyUp="_identifier_TextBox_KeyUp"/>
         <Button Content="✕"
                 Click="_clearFilter_Button_Click"/>
      </StackPanel>
      <Grid Grid.Row="1">
         <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
         </Grid.ColumnDefinitions>
         <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
         </Grid.RowDefinitions>
         <Button Grid.Row="0"
                 Grid.Column="0"
                 Grid.ColumnSpan="2"
                 Content="{loc:Loc IdentifierType_Username}"
                 Tag="Username"
                 Click="_insertIdentifierType_Button_Click"/>
         <Button Grid.Row="0"
                 Grid.Column="2"
                 Grid.ColumnSpan="2"
                 Content="{loc:Loc IdentifierType_Email}"
                 Tag="Email"
                 Click="_insertIdentifierType_Button_Click"/>
         <Button Grid.Row="0"
                 Grid.Column="4"
                 Grid.ColumnSpan="2"
                 Content="{loc:Loc IdentifierType_Passkey}"
                 Tag="Passkey"
                 Click="_insertIdentifierType_Button_Click"/>
         <Button Grid.Row="1"
                 Grid.Column="0"
                 Grid.ColumnSpan="3"
                 Content="{loc:Loc IdentifierType_PhoneNumber}"
                 Tag="Phone Number"
                 Click="_insertIdentifierType_Button_Click"/>
         <Button Grid.Row="1"
                 Grid.Column="3"
                 Grid.ColumnSpan="3"
                 Content="{loc:Loc IdentifierType_AuthenticatorApp}"
                 Tag="Authentificator App"
                 Click="_insertIdentifierType_Button_Click"/>
      </Grid>
      <ListBox Name="_identifiers_LB"
               Grid.Row="2"
               SelectionChanged="_identifiers_LB_SelectionChanged"/>
   </Grid>
</Window>
