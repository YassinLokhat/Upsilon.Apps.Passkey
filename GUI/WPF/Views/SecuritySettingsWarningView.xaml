<Window x:Class="Upsilon.Apps.Passkey.GUI.WPF.Views.SecuritySettingsWarningView"
        x:ClassModifier="internal"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:viewmodels="clr-namespace:Upsilon.Apps.Passkey.GUI.WPF.ViewModels"
        xmlns:loc="clr-namespace:Upsilon.Apps.Passkey.GUI.WPF.Localization"
        d:DataContext="{d:DesignInstance Type=viewmodels:SecuritySettingsWarningViewModel}"
        mc:Ignorable="d"
        WindowStartupLocation="CenterScreen"
        ResizeMode="CanResize"
        Title="{Binding Title}"
        MinHeight="360"
        MinWidth="560"
        Height="420"
        Width="640">
   <Grid Margin="16">
      <Grid.RowDefinitions>
         <RowDefinition Height="*"/>
         <RowDefinition Height="Auto"/>
      </Grid.RowDefinitions>

      <ScrollViewer Grid.Row="0"
                    VerticalScrollBarVisibility="Auto">
         <ItemsControl ItemsSource="{Binding Issues}">
            <ItemsControl.ItemTemplate>
               <DataTemplate>
                  <Border Padding="10,10"
                          BorderBrush="{DynamicResource {x:Static SystemColors.ActiveBorderBrushKey}}"
                          BorderThickness="0,0,0,1">
                     <StackPanel>
                        <TextBlock Text="{Binding Title}"
                                   FontWeight="SemiBold"
                                   FontSize="14"
                                   TextWrapping="Wrap"
                                   Foreground="{DynamicResource WarningBrush}"/>
                        <TextBlock Text="{Binding Description}"
                                   Margin="0,5,0,0"
                                   TextWrapping="Wrap"/>
                     </StackPanel>
                  </Border>
               </DataTemplate>
            </ItemsControl.ItemTemplate>
         </ItemsControl>
      </ScrollViewer>

      <StackPanel Grid.Row="1"
                  Orientation="Horizontal"
                  HorizontalAlignment="Right">
         <Button Content="{loc:Loc Menu_UserSettings}"
                 Click="_openUserSettings_Click"/>
         <Button Content="{loc:Loc Menu_AppSettings}"
                 Click="_openAppSettings_Click"/>
         <Button Content="{loc:Loc Button_OK}"
                 IsDefault="True"
                 Click="_ok_Click"/>
      </StackPanel>
   </Grid>
</Window>
