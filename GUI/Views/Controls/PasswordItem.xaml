<UserControl x:Class="Upsilon.Apps.Passkey.GUI.Views.Controls.PasswordItem"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Upsilon.Apps.Passkey.GUI.Views.Controls"
             xmlns:controls="clr-namespace:Upsilon.Apps.Passkey.GUI.ViewModels.Controls"
             d:DataContext="{d:DesignInstance Type=controls:PasswordItemViewModel}"
             mc:Ignorable="d">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>
        <Label Grid.Column="0"
               VerticalContentAlignment="Center"
               Content="{Binding Index}"/>
        <local:VisiblePasswordBox x:Name="_password_VPB"
                                  Margin="5"
                                  Grid.Column="1"/>
        <Button Grid.Column="2"
                Content="🠉"
                Click="_upButton_Click"/>
        <Button Grid.Column="3"
                Content="🠋"
                Click="_downButton_Click"/>
        <Button Grid.Column="4"
                Content="✕"
                Click="_deleteButton_Click"/>
    </Grid>
</UserControl>
