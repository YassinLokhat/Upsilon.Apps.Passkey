<UserControl x:Class="Upsilon.Apps.Passkey.GUI.Views.Controls.AccountIdentifiantItem"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Upsilon.Apps.Passkey.GUI.Views.Controls"
             xmlns:controls="clr-namespace:Upsilon.Apps.Passkey.GUI.ViewModels.Controls"
             d:DataContext="{d:DesignInstance Type=controls:AccountIdentifiantItemViewModel}"
             mc:Ignorable="d">
    <Grid>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBox Grid.Column="0"
                     Background="{Binding IdentifiantBackground}"
                     Text="{Binding Identifiant, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
            <Button Grid.Column="1"
                    Content="🠉"
                    Click="_upButton_Click"/>
            <Button Grid.Column="2"
                    Content="🠋"
                    Click="_downButton_Click"/>
            <Button Grid.Column="3"
                    Content="✕"
                    Click="_deleteButton_Click"/>
            <Button Grid.Column="4"
                    Content="▣"
                    Click="_qrCodeButton_Click"/>
        </Grid>
    </Grid>
</UserControl>
