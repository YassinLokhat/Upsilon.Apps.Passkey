<UserControl x:Class="Upsilon.Apps.Passkey.GUI.Views.Controls.AccountView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Upsilon.Apps.Passkey.GUI.Views.Controls"
             xmlns:controls="clr-namespace:Upsilon.Apps.Passkey.GUI.ViewModels.Controls"
             d:DataContext="{d:DesignInstance Type=controls:AccountViewModel}"
             mc:Ignorable="d">
    <DockPanel VerticalAlignment="Stretch">
        <GroupBox Header="{Binding AccountId}"
                  DockPanel.Dock="Top">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Label Grid.Row="0"
                        Grid.Column="0"
                        Content="Label : "/>
                <TextBox Grid.Row="0"
                            Grid.Column="1"
                            Text="{Binding Label, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                            Background="{Binding LabelBackground}"/>
                <local:AccountIdentifiantsView x:Name="_identifiants_AIV"
                                               Grid.Row="1"
                                               Grid.Column="0"
                                               Grid.ColumnSpan="2"/>
                <Label Grid.Row="2"
                        Grid.Column="0"
                        Content="Notes : "/>
                <TextBox Grid.Row="2"
                            Grid.Column="1"
                            Text="{Binding Notes, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                            Background="{Binding NotesBackground}"
                            AcceptsReturn="True"
                            AcceptsTab="True"/>
                <GroupBox Header="Options"
                            Grid.Row="3"
                            Grid.Column="0"
                            Grid.ColumnSpan="2">
                    <StackPanel>
                        <StackPanel Orientation="Horizontal">
                            <CheckBox IsChecked="{Binding RemindPasswordUpdate}"
                                        Content="Remind to update password every"/>
                            <TextBox Width="25"
                                        HorizontalContentAlignment="Center"
                                        TextChanged="_value_TextBox_TextChanged"
                                        PreviewTextInput="_value_TextBox_PreviewTextInput"
                                        DataObject.Pasting="_value_TextBox_Pasting"
                                        Text="{Binding RemindPasswordUpdateDelay, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
                            <Label Margin="0">months.</Label>
                        </StackPanel>
                        <CheckBox IsChecked="{Binding WarnPasswordLeak}">Warn if the password leaked.</CheckBox>
                    </StackPanel>
                </GroupBox>
            </Grid>
        </GroupBox>
    </DockPanel>
</UserControl>
