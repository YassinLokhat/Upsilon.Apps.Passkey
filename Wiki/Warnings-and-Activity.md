# Warnings and Activity

This page has been renamed. See **[[Alerts and Activity]]**.

The product now uses **alerts** (`IAlert`, `AlertKinds`, `AlertBroker`) instead of the old `IWarning` / `WarningType` model. Legacy vaults that still store `WarningsToNotify` as `WarningType` flags are migrated on load — details are on that page.
